import PropTypes from 'prop-types';
import styles from './Preloader.module.scss';

const Preloader = () => {
    return (
        <>Loading...</>
    );
}

// Preloader.propTypes = {
//     text: PropTypes.
// }

export default Preloader;